<li>
	{{name}}

	<button class="change-devour btn btn-success" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devoured}}Devour{{else}}Restock{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">Remove</button>
</li>